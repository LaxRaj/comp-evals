# Copy to .env and fill in. .env is gitignored — never commit real keys.
# The spike (M0) only needs ANTHROPIC_API_KEY. A full `run` needs both keys below.
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
