# Copy to .env and fill in. .env is gitignored — never commit real keys.
# M0 (the spike) only needs ANTHROPIC_API_KEY. M2 (multi-model runner) needs all three.
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=...
